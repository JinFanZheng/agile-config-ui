# ITER-16 证据 — 侧栏导航分组

基线：@17a6099（typecheck/lint ✓，vitest 118，e2e 37）。收口后：typecheck ✓ / lint ✓ / vitest 118 / e2e **37** / build ✓。

## 结构

概览（置顶不分组）→ **配置管理**（应用/发布历史）→ **运维监控**（客户端/节点/服务注册/系统日志）→ **权限管理**（用户/角色）→ 接入指南+设置（沉底不分组）。分组标签复用主题切换器的 10px 弱化样式；权限过滤后组内无可见项 → 整组含标签隐藏；移动端抽屉同结构。

## 验证

- e2e `access.spec` 扩展：只读用户下 `配置管理`/`运维监控` 标签可见、`权限管理` 标签 **隐藏**（整组 fail-closed）；存量以链接名断言的用例全部兼容通过。
- 截图：`nav-grouped-graphite.png` / `nav-grouped-navy-console.png`（浅深两主题，DOM 断言三标签俱在）。
- 过程记录：发布链路全流程用例在全量并行下两次逼近 30s 超时闪挂（隔离 ~14s 稳定通过），已 `test.setTimeout(60_000)` 加固并在卡内如实记录；加固后全量两连绿（37/37）。
