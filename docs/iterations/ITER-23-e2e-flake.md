# ITER-23 e2e 抗抖加固

> 状态：✅ VERIFIED（2026-09-08 双轮全绿 flaky=0 + 后续三轮全量含 47 用例零 flaky）。
> 变更分类：测试基础设施（无产品代码变更）。

## 1. 抖动实录（本会话 3 起，均为隔离复跑秒过）

| 用例 | 次数 | 形态 | 已采取措施 |
| --- | --- | --- | --- |
| 发布链路全流程 | 2 | 并行负载下逼近 30s 默认超时 | 已单独 setTimeout(60s) |
| 接入指南（六节+复制） | 1 | 首跑挂、复跑过 | 无 |
| 部分发布（ids） | 1 | 重建镜像后首跑 1.0s 挂 | 无 |

根因共性：本地 2 worker 共享 dev/nginx + 后端，时序敏感断言在负载尖峰偶发超时；CI 已有 retries:1，本地为 0。

## 2. 方案

- `retries: 1` 全环境统一（本地与 CI 对齐；Playwright 会把首败重跑通过的计为 flaky 并在报告标注，不掩盖可复现的真回归）
- 原始 sleep 审计：全仓仅 4 处 waitForTimeout（均为 150–800ms 交互后稳定等待，非等待条件），不动
- 验证：全量 e2e 连续两轮全绿（flaky=0）

## 3. 验收记录（2026-09-08）

- `retries: 1` 全环境（本地原为 0）；全量连跑两轮 44/44 全绿 flaky=0；后续 ITER-24 联测三轮（44/47/47）零 flaky。
- 原始 sleep 审计确认全仓仅 4 处交互后短等待（150–800ms），非条件等待，维持不动。
