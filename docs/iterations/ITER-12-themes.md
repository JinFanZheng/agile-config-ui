# ITER-12 盲盒皮肤：新增 5 套主题

> 状态：验收中（EVIDENCE_READY，2026-09-08 实现与确定性验证全绿，等待用户验收）。
> 变更分类：中改（单事实源文件增量 + 注册点接线 + 既有硬编码修复）。
> 依赖：ITER-02 主题引擎、ITER-09 设置中心（均已就绪）。

## 1. 目标与范围

- G1（盲盒）：新增 5 套主题（选型由实现者定），注册进主题引擎后在顶栏切换器与 /settings 主题五→十选一自动出现，即时生效+持久化+防闪屏全链路成立
- G2（语义不变量）：十套主题下环境语义色（DEV 绿/TEST 橙/PROD 红）与状态语义（warning/success/danger/info）色相不串、可读（WCAG AA）
- G3（深色 monaco）：深色判定从硬编码 `theme === 'navy-console'` 改为注册表驱动（`isDarkTheme`），全部深色主题下 monaco Editor/DiffEditor 用深色底
- G4（可读性验证）：确定性对比度断言（WCAG 数学，非目测）覆盖十主题的关键令牌组合

### 范围外：design/previews 静态稿（新主题无静态稿留档，以实现为准）；主题排序调整；主题管理 UI（如自定义色）。

## 2. 选型（盲盒内容，与既有 5 套互补：现有 4 浅 1 深 → 补 1 浅 4 深，凑 5/5 平衡）

| 主题 id        | 名称 | 气质                       | 深浅 | 关键色                       |
| -------------- | ---- | -------------------------- | ---- | ---------------------------- |
| `obsidian`     | 曜石 | OLED 纯黑、终端感、白强调  | 深   | 底 #000000 / 白 #FAFAFA      |
| `violet-night` | 紫夜 | 深紫罗兰、夜域感           | 深   | 底 #120F1E / 紫罗兰 #A78BFA  |
| `sakura`       | 樱粉 | 浅玫瑰、最柔圆角           | 浅   | 底 #FDF7F9 / 品红 #B52A86（防混淆后） |
| `mocha`        | 摩卡 | 暖棕深色、奶油金强调       | 深   | 底 #16120B / 奶油金 #EAD9AE  |
| `forest`       | 森夜 | 深绿、月光银强调（与摩卡拉开） | 深 | 底 #0C1410 / 银绿 #D5E8DA     |

强调色避开语义色族（绿/橙黄/红/蓝），避免与 success/warning/danger/info 混淆。

## 3. 任务

| Task | 内容                                                                                    | Verify                                   |
| ---- | --------------------------------------------------------------------------------------- | ---------------------------------------- |
| T-01 | 5 套令牌块（src/index.css）+ 注册（themes.ts 含 isDarkTheme、strings/theme.ts、index.html BG 表） | 单测（注册表完整性）+ 对比度脚本          |
| T-02 | JsonView/KvView 深色判定改 isDarkTheme；e2e 扩展十主题切换；文案/注释/事实源文档同步       | e2e 全绿 + 逐主题截图                    |

## 4. Gate 要点

GR-1 baseline（@54fbf91 全绿）；GR-2 L0+L1+L-Real；令牌唯一事实源 index.css（含 index.html BG 互指同步）；handoff §7.1 主题清单回写；语义不变量逐主题断言（G2/G4）。

## 5. 验收记录（2026-09-08）

- Baseline：@54fbf91（typecheck/lint ✓，vitest 103，e2e 29）。
- 实现：5 令牌块（index.css）+ 注册（themes.ts 含 isDarkTheme/DARK_THEMES、strings、index.html THEMES+BG 表）+ JsonView/KvView 深浅判定去硬编码 + ThemeSwitcher 注释同步；handoff §7.1 主题清单/README 同步为十主题。
- 选型迭代（防混淆断言驱动）：樱粉强调 玫红→品红（与 danger 拉开 ≥20° 色相）；森夜强调 月光金→月光银白（与摩卡金拉开身份）；樱粉 env-dev/test 加深（非文本 3:1 留余量）。
- 验证：对比度/色相/交叉一致脚本 PASS（新主题硬卡 AA、既有 5 套 16 条边际项记录为基线审计不改已验收外观）；vitest 110（+7）；e2e 34（+5）；monaco 底色实测四深一浅全对；4 深色主题刷新逐帧零白帧；15 张截图 + 2 次视觉抽检。
- 待用户验收：十主题逐个切换过目（建议重点：曜石极黑观感、樱粉柔圆角、摩卡暖棕、森夜银强调）。
