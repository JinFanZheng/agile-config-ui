# ITER-24 体验小项：侧栏折叠 / 登录落地页 / 分页大小 + 指南全宽

> 状态：验收中（EVIDENCE_READY，2026-09-08 实现与全量验证绿，等待用户过目）。
> 需求出处：用户指示 backlog 第 4 项（"测试好，注意移动端"）+ 指南宽度评估。

## 1. 交付

| 项 | 说明 |
| --- | --- |
| 侧栏折叠 | 桌面停靠态 w-48↔w-14 图标栏（就地开关持久化；文字转 sr-only 保留可访问名 + title 提示）；移动端抽屉恒为完整宽度不受影响 |
| 登录后打开 | 概览/应用二选一（设置页）；**登录跳转收口**：发现并修复 RedirectIfAuthed 与 LoginPage 双导航竞速（守卫硬编码 /' 打回落地偏好），现统一为 from 回跳 > 落地偏好 单一来源 |
| 列表每页条数 | 20/50/100（默认 20）；应用/日志/用户/服务注册四页 reactive 接入（改设置即时生效并进 queryKey）；客户端页刻意固定 50 |
| 接入指南全宽 | 移除 max-w-5xl 与其他页面一致（移动端审计复核无回归） |

## 2. 验收记录（2026-09-08）

- 单测 131（+1：三字段 setter/持久化/非法回退）；e2e 新增 prefs.spec 3 用例（折叠持久与可访问名/落地页含 from 优先级/分页 waitForRequest 断言 pageSize=50）。
- 全量 e2e（retries:1）**47/47** 零 flaky；移动端审计 16 路由 pageOverflow 全 0（指南全宽与折叠无移动端回归）。
- 落地页竞速缺陷为真实 bug（登录后跳转由守卫竞速决定而非意图），probe-landing.mjs 导航序列证据（/apps 被 Navigate to="/" 打回）。
- 登录期 `GET /SSO/LoginUrl` 400 为 SSO 探测显隐的预期行为（未配置实例返回 400 隐藏入口），非缺陷。
- 证据：docs/evidence/ITER-24/（折叠两态截图 56px 实测/设置页七组 radiogroup/指南全宽/probe）。
