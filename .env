# 提交到仓库的非敏感默认值（真实凭证见 .env.development / .env.e2e，均已 gitignore）

# 前端请求 API 的基地址：留空 = 同源（dev 走 vite 代理，生产走 nginx 同源反代）
VITE_API_BASE=

# 开发代理 / E2E 指向的 AgileConfig 后端地址
VITE_BACKEND_URL=http://localhost:5017
