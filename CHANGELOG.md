# Changelog

本项目的显著变更记录于此。格式参照 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本遵循语义化。

## [1.1.0] - 2026-09-08

自 v1.0.0 以来的变更（迭代记录见 `docs/iterations/`，共 17 张）。

### 新增

- **设置中心**（`/settings`）：浏览器本地个性化——主题、界面字号、编辑器字号、动效、配置页默认视图、编辑器自动换行、登录后落地页、列表每页条数、桌面侧栏折叠，全部即时生效 + 持久化。
- **十主题引擎**：新增曜石 / 紫夜 / 樱粉 / 摩卡 / 森夜五套皮肤（5 浅 + 5 深），切换器与设置页按浅/深分组；「跟随系统」档支持自定义深浅映射（默认 深蓝中控 / 石墨）；十主题全部通过 WCAG 对比度与语义色相不变量断言。
- **接入指南**（`/guide`）：面向接入方开发者的产品内文档——快速开始 / C# SDK / 依赖注入与 IConfiguration（`AddAgileConfig` + `IOptionsMonitor` 热更新）/ 服务注册与发现 / 格式与归一化实测坑位 / FAQ，代码块一键复制，全部结论实测验证。
- **AI 助手接入入口**：`/llms.txt` 与 `/llms-full.txt` 与指南同源生成（一致性由单测锁定），AI 编码助手可直接抓取完成客户端接入。
- **SSO/OIDC 完整链路**：登录页入口显隐 + 服务端可配按钮文案 + 回调自动兑换登录（真实 Keycloak 端到端验证）；一键测试工具 `scripts/sso-test.sh`。
- **侧栏导航分组**：概览置顶 + 配置管理 / 运维监控 / 权限管理三组（无权限整组隐藏），支持折叠为图标栏。
- **H5 移动端适配**：顶栏、抽屉导航、对比视图 inline 单栏等全面适配，附专项移动端 E2E 回归。
- **交付流水线**：GitHub Actions 三工作流——CI（typecheck/lint/单测/构建）、E2E（真实后端容器 + 生产产物冒烟）、Docker（GHCR 多架构镜像，tag 触发）；`docker-compose.image.yml`（免克隆镜像部署，数据库可参数化）与 `docker-compose.mysql.yml`（MySQL 栈）。

### 修复

- 深色主题刷新白闪（FOUC）：引导脚本按主题同步铺底，逐帧验证零白帧。
- 生产构建整页崩溃：CSS 压缩产生的 3 位 hex 触发 monaco 主题严格校验抛错。
- 应用更新后旧标签页加载失败露出英文默认错误页：中文路由错误页 + 过期资源自动刷新自愈。
- 登录后跳转由守卫与页面双导航竞速决定（落地偏好失效）：收口为单一来源（回跳地址 > 偏好）。
- 移动端顶栏环境切换器被压缩裁切（PROD 不可见不可点）。

### 工程质量

- 测试规模：vitest 134 / Playwright 47（dev 与 nginx 生产产物双覆盖 + 生产哨兵 + 移动端专项），E2E 抗抖策略（限并发 + 统一重试）。
- 令牌对比度/色相不变量/三处事实源交叉一致的确定性断言脚本（`docs/evidence/ITER-12/check-contrast.mjs`）。

## [1.0.0] - 2026-09-08

首个正式版本：86 个管理端点全部适配——多主题管理台（应用 / 配置三视图 / 发布链路 diff / 回滚 / 运维视图 / 权限体系 / 导入导出 / 环境同步 / 服务注册 / SSO 入口），Docker 镜像与 compose 一键演示。迭代详情见 `docs/PROGRESS.md`。
