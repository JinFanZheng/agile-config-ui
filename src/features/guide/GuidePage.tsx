import { PlaceholderPage } from '../PlaceholderPage'
import { guideStr } from '../../strings/guide'

/** 接入指南：面向接入方开发者的产品内文档页（ITER-10） */
export function GuidePage() {
  return <PlaceholderPage title={guideStr.title} description={guideStr.placeholder} />
}
